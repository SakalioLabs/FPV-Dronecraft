# 仿真分析脚本

这里保存用于 `sim/lab` 的数据抓取、解析、缓存和验证脚本。脚本可以生成或更新 `docs/data/*.csv`，并可能在本地创建 `docs/data/raw/**` 缓存。

约定：

- `docs/data/raw/**` 是本地缓存，不进入 Git。
- 新脚本应尽量输出可审计的 CSV packet，并在对应文档中说明来源。
- 脚本结果用于仿真实验线，不直接代表可玩分支参数变更。
