# 仿真数据包

这里保存 `sim/lab` 使用的顶层 CSV 数据包和校准线索。文件主要由 `docs/scripts/*.py` 生成，用于支撑公开数据源、物理模型验证和后续参数实验。

约定：

- 只提交整理后的 CSV packet。
- 不提交 `raw/`、下载缓存、第三方依赖或大体积原始数据。
- 调参时优先引用具体 CSV 文件和字段，不只引用 Markdown 摘要。
- 运行脚本生成新数据后，先确认文件体积、来源和可复现路径，再提交。
